---

- name: Disable daily upgrades
  systemd:
    name: "{{ item }}"
    state: disabled
    daemon-reload: yes
  with_items:
    - 'apt-daily-upgrade.timer'
    - 'apt-daily.timer'
    - 'apt-daily.service'
